<% plans_for_date.each do |plan| %>
  <div class="plan color-<%= plan.owner_id % 10 %>">
    <strong>
      <%= link_to "#{plan.title} (#{plan.start_time.strftime("%k:%M")} - #{plan.end_time.strftime("%k:%M")})", plan_path(plan) %>
    </strong>
  </div>
<% end %>