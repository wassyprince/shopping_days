<h2>🛒 今日の買い物リスト</h2>
<% if @shopping_lists.any? %>
  <ul>
    <% @shopping_lists.each do |list| %>
      <li><%= link_to list.title, shopping_list_path(list) %></li>
    <% end %>
  </ul>
<% else %>
  <p>買い物リストはありません</p>
<% end %>